<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

<h3 class="font-extrabold text-5xl">Hello, world!</h3>
@if(location is null)
{
    <button type="button" class="flex items-center justify-center px-8 py-3 font-semibold rounded dark:bg-violet-400 dark:text-gray-800 divide-gray-700">
        @ChildContent
    </button>
}
else
{  
    @if (location.ToString().ToLower().StartsWith("left"))
    {
        <button type="button" class="px-8 py-3 font-semibold rounded dark:bg-violet-400 dark:text-gray-800">
            <i class="fas fa-user mr-2"></i> @ChildContent
        </button>
    }
    @if(location.ToString().ToLower().StartsWith("right"))
    {
        @*<button type="button" class="px-8 py-3 font-semibold rounded dark:bg-violet-400 dark:text-gray-800" style="padding-top: revert-layer; padding-right: 100px; height: 45px; padding-left: 100px; width: 280px;>
            @ChildContent
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M5.25 14.25h13.5m-13.5 0a3 3 0 01-3-3m3 3a3 3 0 100 6h13.5a3 3 0 100-6m-16.5-3a3 3 0 013-3h13.5a3 3 0 013 3m-19.5 0a4.5 4.5 0 01.9-2.7L5.737 5.1a3.375 3.375 0 012.7-1.35h7.126c1.062 0 2.062.5 2.7 1.35l2.587 3.45a4.5 4.5 0 01.9 2.7m0 0a3 3 0 01-3 3m0 3h.008v.008h-.008v-.008zm0-6h.008v.008h-.008v-.008zm-3 6h.008v.008h-.008v-.008zm0-6h.008v.008h-.008v-.008z" />
            </svg>
            <i class="fa-solid fa-network-wired" style="color: #ffffff;"></i>
            <i class="fas fa-arrow-right ml-2"></i> 
        </button>*@
        <button type="button" class="btn">
           <i class="fa-solid fa-network-wired" style="color: #ffffff;"></i>
        </button>
    }
}

@code {
    [Parameter]
    public string? location { get; set; }

    [Parameter] 
    public RenderFragment? ChildContent { get; set; }
}